# Keeper – copy to .env and fill for local run or Railway Variables.
# See apps/keeper/README.md for worker modes and required keys.

# -----------------------------------------------------------------------------
# Deploy Executor (HTTP service) – required
# -----------------------------------------------------------------------------
PHAROS_ATLANTIC_RPC_URL=
DEPLOYER_PRIVATE_KEY=
TRANCHE_FACTORY=

# Optional: contract workspace (in Docker use /app/contracts)
# CONTRACTS_WORKSPACE_DIR=
# KEEPER_CONTRACTS_DIR=

# Optional: role addresses for new vaults
# DEPLOYER_OPERATOR=
# DEPLOYER_GUARDIAN=
# DEPLOYER_STRATEGIST=
# DEPLOYER_MANAGER_ADMIN=

# Optional: key for update-rate endpoint (fallback: PRIVATE_KEY, then DEPLOYER_PRIVATE_KEY)
# ACCOUNTANT_UPDATER_PRIVATE_KEY=

# Optional: auth and server
# DEPLOY_EXECUTOR_TOKEN=
# DEPLOY_EXECUTOR_HOST=0.0.0.0
# DEPLOY_EXECUTOR_PORT=8787

# -----------------------------------------------------------------------------
# Operator Worker (async step executor) – required
# Polls Supabase operator steps, runs Foundry scripts, and writes results back.
# -----------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPERATOR_ADMIN_ADDRESSES=

# Optional
# KEEPER_WORKER_INTERVAL_MS=3000

# -----------------------------------------------------------------------------
# Rate Updater (scheduled exchange-rate tick) – required when using start/start:once
# -----------------------------------------------------------------------------
# PHAROS_ATLANTIC_RPC_URL=
# PRIVATE_KEY=
# VAULT=
# ACCOUNTANT=
# ASSET=

# Optional
# MIN_UPDATE_BPS=1
# ALLOW_PAUSE_UPDATE=false
# KEEPER_INTERVAL_MS=300000
# KEEPER_RUN_ONCE=false
