# Pontus Vault contracts env template (contracts/.env)
# Usage:
#   cd contracts
#   cp .env.example .env

###############################################################################
# 1) Shared Across Scripts/Tests (fill for all contract workflows)
###############################################################################
PHAROS_ATLANTIC_RPC_URL=

###############################################################################
# 2) Deploy Script: script/DeployInfra.s.sol
# Required: PRIVATE_KEY
# Optional: OWNER (defaults to vm.addr(PRIVATE_KEY))
# Optional fields can be left empty to use script defaults.
###############################################################################
PRIVATE_KEY=
OWNER=

###############################################################################
# 3) Deploy Script: script/DeployTrancheVault.s.sol
# Required: TRANCHE_FACTORY, ASSET
# Optional defaults:
# - OPERATOR defaults to OWNER
# - GUARDIAN defaults to OWNER
# - STRATEGIST defaults to OPERATOR
# - MANAGER_ADMIN defaults to OWNER
# - BALANCER_VAULT defaults to zero address
###############################################################################
OPERATOR=
GUARDIAN=
STRATEGIST=
MANAGER_ADMIN=

# DeployTrancheVault.s.sol inputs
# Pre-filled from latest Pharos Atlantic infra deployment (proxy addresses).
# Override these values if you redeploy infra.
TRANCHE_FACTORY=0x7fBaFFA7fba0C6b141cf06B01e1ba1f6FB2350F8
# TrancheRegistry proxy is used by indexer wiring script:
# contracts/script/update-indexer-subgraph.sh --registry "$TRANCHE_REGISTRY" --start-block <block>
TRANCHE_REGISTRY=0x341A376b59c86A26324229cd467A5E3b930792C6
# Default asset for current Atlantic demos (USDT).
ASSET=0xE7E84B8B4f39C507499c40B4ac199B050e2882d5
BALANCER_VAULT=
SENIOR_RATE_PER_SECOND_WAD=0
MAX_SENIOR_RATIO_BPS=8000
MAX_RATE_AGE=3600
SENIOR_TOKEN_NAME=Pontus Vault Senior
SENIOR_TOKEN_SYMBOL=ptS
JUNIOR_TOKEN_NAME=Pontus Vault Junior
JUNIOR_TOKEN_SYMBOL=ptJ
BORING_VAULT_NAME=Pontus Vault Base
BORING_VAULT_SYMBOL=PTVB
BORING_VAULT_DECIMALS=18
ACCOUNTANT_SHARE_PRICE=1000000
ACCOUNTANT_UPPER_BOUND_BPS=11000
ACCOUNTANT_LOWER_BOUND_BPS=9000

###############################################################################
# 4) Keeper Script: script/UpdateExchangeRate.s.sol
# Required: VAULT, ACCOUNTANT, ASSET
# Optional: MIN_UPDATE_BPS, ALLOW_PAUSE_UPDATE
###############################################################################
VAULT=
ACCOUNTANT=
MIN_UPDATE_BPS=1
ALLOW_PAUSE_UPDATE=false
