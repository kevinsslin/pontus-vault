{
  "name": "@pti/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "fmt": "forge fmt",
    "deploy": "forge script script/Deploy.s.sol --broadcast --rpc-url $PHAROS_RPC_URL"
  }
}
