{
  "name": "@pti/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deps": "./script/install-deps.sh",
    "build": "forge build",
    "test": "forge test",
    "lint": "forge fmt --check",
    "fmt": "forge fmt",
    "deploy": "forge script script/DeployInfra.s.sol --broadcast --rpc-url $PHAROS_RPC_URL",
    "deploy:infra": "forge script script/DeployInfra.s.sol --broadcast --rpc-url $PHAROS_RPC_URL",
    "deploy:vault": "forge script script/DeployTrancheVault.s.sol --broadcast --rpc-url $PHAROS_RPC_URL",
    "verify:contract": "./script/verify-contract.sh"
  }
}
