# Pontus Vault contracts script environment template.
# Keep this file as documentation only. Put real secrets in your local `.env`.
# Use: `cp script/.env.example script/.env` then edit values.

###############################################################################
# Shared (used by multiple scripts)
###############################################################################
PHAROS_RPC_URL=https://atlantic.dplabs-internal.com
PRIVATE_KEY=0x
OWNER=0x

###############################################################################
# script/DeployInfra.s.sol
# Command: forge script script/DeployInfra.s.sol --broadcast --rpc-url $PHAROS_RPC_URL
###############################################################################
# Required
# - PRIVATE_KEY
# - OWNER (optional in script; defaults to vm.addr(PRIVATE_KEY))

###############################################################################
# script/DeployTrancheVault.s.sol
# Command: forge script script/DeployTrancheVault.s.sol --broadcast --rpc-url $PHAROS_RPC_URL
###############################################################################
# Required
TRANCHE_FACTORY=0x
ASSET=0x

# Optional role overrides (default to OWNER if empty)
OPERATOR=0x
GUARDIAN=0x
MANAGER=0x

# Optional product params
PARAMS_HASH=0x
SENIOR_RATE_PER_SECOND_WAD=0
MAX_SENIOR_RATIO_BPS=8000

# Optional tranche token metadata
SENIOR_TOKEN_NAME=Pontus Vault Senior
SENIOR_TOKEN_SYMBOL=ptS
JUNIOR_TOKEN_NAME=Pontus Vault Junior
JUNIOR_TOKEN_SYMBOL=ptJ

# Optional BoringVault metadata + accountant bounds
BORING_VAULT_NAME=Pontus Vault Base
BORING_VAULT_SYMBOL=PTVB
BORING_VAULT_DECIMALS=18
ACCOUNTANT_SHARE_PRICE=1000000
ACCOUNTANT_UPPER_BOUND_BPS=11000
ACCOUNTANT_LOWER_BOUND_BPS=9000

###############################################################################
# script/verify-contract.sh (Blockscan / Blockscout-compatible verify)
# Command: CONTRACT_ADDRESS=0x... CONTRACT_ID=src/...:Contract pnpm verify:contract
###############################################################################
# Optional (defaults are set in verify-contract.sh)
CHAIN_ID=688689
VERIFY_VERIFIER=blockscout
VERIFY_VERIFIER_URL=https://api.socialscan.io/pharos-atlantic-testnet/v1/explorer/command_api/contract
BLOCKSCAN_API_KEY=
VERIFY_WATCH=1

# Required for each verify run
CONTRACT_ADDRESS=0x
CONTRACT_ID=src/tranche/TrancheRegistry.sol:TrancheRegistry

# Optional for constructor-based contracts (ABI encoded hex)
CONSTRUCTOR_ARGS=
